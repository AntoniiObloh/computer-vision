# Computer Vision — pinned to the versions the notebook was written against.
# Install:  .venv/bin/pip install -r requirements.txt

# contrib build, not plain opencv-python: without it there is no cv2.legacy.* and no GOTURN.
# 4.10 is the last version that still ships GOTURN (removed in 5.0).
opencv-contrib-python==4.10.0.84
numpy==2.4.6
matplotlib==3.10.7
pillow==12.0.0

# Jupyter (notebook + the kernel PyCharm/Jupyter run the code in)
notebook==7.3.2
ipykernel==6.29.5

# HandTrackingProject/hand_tracking_min.py — MediaPipe Tasks HandLandmarker.
# Does not pin numpy or OpenCV, so the versions above stay as they are.
#
# Pinned to 1.0.0, and NOT 1.0.1: on macOS arm64 1.0.1 aborts while opening the
# graph with "graph_service.h:139] Check failed: service_ Service is
# unavailable" from DrishtiMetalHelper, because TensorsToDetectionsCalculator
# asks for the GPU service that the Python layer never registers. Neither
# delegate=CPU nor delegate=GPU avoids it, and MEDIAPIPE_DISABLE_GPU has no
# effect. 1.0.0 loads the same task bundle and runs detect() fine.
mediapipe==1.0.0
