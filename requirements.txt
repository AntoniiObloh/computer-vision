# Computer Vision — pinned to the versions the notebook was written against.
# Install:  .venv/bin/pip install -r requirements.txt

# contrib build, not plain opencv-python: without it there is no cv2.legacy.* and no GOTURN.
# 4.10 is the last version that still ships GOTURN (removed in 5.0).
opencv-contrib-python==4.10.0.84
numpy==2.4.6
matplotlib==3.10.7
pillow==12.0.0

# Jupyter (notebook + the kernel PyCharm/Jupyter run the code in)
notebook==7.3.2
ipykernel==6.29.5

# HandTrackingProject/hand_tracking_min.py — MediaPipe Tasks HandLandmarker.
# Does not pin numpy or OpenCV, so the versions above stay as they are.
#
# NOT 1.0.x on macOS: 1.0.1 aborts while opening the graph with
# "graph_service.h:139] Check failed: service_ Service is unavailable" from
# DrishtiMetalHelper — the GPU service is never registered, yet
# TensorsToDetectionsCalculator still asks for it. delegate=CPU does not help.
# 0.10.35 is the newest release that runs on macOS arm64 and still allows numpy 2.
mediapipe==0.10.35
